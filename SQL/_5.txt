Use all of the functions below, find out what they do, write a description for them here or in the comments of the code. Add a meaningful column alias

CONCAT(COLUMN1, COLUMN2) AS ALIAS_NAME
 LOWER(COLUMN)
 UPPER(COLUMN)
 TRIM(COLUMN)
 LENGTH(COLUMN)
 SUBSTRING(COLUMN, START, LENGTH)
 ROUND(COLUMN, DECIMAL_PLACES)
 DATE_ADD(COLUMN, INTERVAL EXPRESSION UNIT)
 DATE_SUB(COLUMN, INTERVAL EXPRESSION UNIT)
 FORMAT(COLUMN, FORMAT_STRING)

SELECT 
 LOWER(MEAL_NAME) AS LOWERCASE_NAMES ,
 UPPER(MEAL_NAME) AS UPPERCASE_NAMES,
 TRIM(MEAL_NAME) AS TRIMMEDNAMES,
 LENGTH(MEAL_NAME) AS LENGHT,
 SUBSTRING(MEAL_NAME, 2,4) AS SUBS,
 DATE_ADD(TIME_ORDER, INTERVAL 1 YEAR) AS 1_YEARLATER,
 DATE_SUB(TIME_ORDER, INTERVAL 1 YEAR) AS 1_YEARBEFORE,
 FORMAT(TIME_ORDER,'AAAA') AS NON_HO_CAPITO_COSA_FA
 FROM MEAL_ORDER

Using DATE_ADD or DATE_SUB and the following SQL

SELECT NOW();


SELECT NOW() AS COURRENT_TIME_2_HOUR_LESS AS NOW                                                      //PRESO L'ORA GMT-1 (NON SO PERCHE)
SELECT DATE_ADD(NOW(), INTERVAL 2 HOUR) AS COURRENT_TIME                                              //PRESO L'ORA CORRENTE
SELECT DATE_ADD(DATE_ADD(NOW(), INTERVAL 2 HOUR), INTERVAL 6.37 HOUR_MINUTE) AS MIDNIGHT              //QUASI PRESO LA MEZZANOTTE

Select your birthday by passing the result into the next item

SELECT DATE_SUB(DATE_ADD(DATE_SUB(NOW(), INTERVAL 25 YEAR),INTERVAL 6 MONTH),INTERVAL 5 DAY) AS MY_BIRTHDAY


i.e. DATE_ADD(DATE_ADD(NOW(), INTERVAL 7 DAY), INTERVAL 2 MONTH) as BIRTHDAY_DATE

If you're feeling really adventurous try to get the time to exactly midnight!

